# ระบบจำกัดจำนวน Music Bot ตาม Package

## 📋 สรุประบบ

ระบบนี้จำกัดจำนวน Music Bot ที่เซิร์ฟเวอร์สามารถใช้งานได้ตาม package ที่ซื้อ

### 🎯 การทำงาน

1. **เมื่อซื้อ Package หลัก** - ระบบจะตั้งค่า `maxMusicBots` ตามแพ็คเกจที่เลือก
2. **เมื่อซื้อ Music Bot Add-on** - ระบบจะเพิ่ม `maxMusicBots` ตามจำนวนที่ซื้อ
3. **เมื่อ Assign Music Bot** - ระบบจะตรวจสอบว่าไม่เกินขีดจำกัดที่กำหนด

---

## 📦 จำนวน Music Bot ตาม Package

### แพ็คเกจหลัก (Main Packages)

| Package | ชื่อ | จำนวน Music Bot | ราคา/เดือน | ราคา/ปี |
|---------|------|-----------------|------------|----------|
| Package 1 | 📦 Starter | **1** ตัว | 79 บาท | 790 บาท |
| Package 2 | 📦 Pro | **2** ตัว | 169 บาท | 1,690 บาท |
| Package 3 | 📦 Premium | **3** ตัว | 299 บาท | 2,990 บาท |
| Package 4 | 📦 Ultra | **5** ตัว | 499 บาท | 4,990 บาท |

### Music Bot Add-ons

| Add-on | จำนวน Bot | ราคา | ราคาเฉลี่ย/Bot |
|--------|-----------|------|----------------|
| Add-on 1 | +3 ตัว | 59 บาท | 19.67 บาท |
| Add-on 2 | +5 ตัว | 89 บาท | 17.8 บาท |
| Add-on 3 | +9 ตัว | 149 บาท | 16.56 บาท |
| Add-on 4 | +15 ตัว | 199 บาท | 13.27 บาท |
| Add-on 5 | +25 ตัว | 250 บาท | 10.0 บาท |

---

## 🔧 การใช้งาน

### 1. ดูสถานะ Music Bot

```
/music-bot-status
```

ผลลัพธ์:
```
🎵 Music Bot Status

🟢 การใช้งาน: 2/5 ตัว (40%)
████░░░░░░

✅ เหลืออีก 3 ตัวที่สามารถใช้งานได้

เซิร์ฟเวอร์นี้มี Music Bot ที่กำลังใช้งาน 2 ตัว

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[รายละเอียด Music Bot แต่ละตัว...]
```

### 2. ซื้อ Package หรือ Add-on

```
/server-buy-package
```

- เลือก **Main Package** เพื่อซื้อแพ็คเกจหลัก (ตั้งค่า maxMusicBots ใหม่)
- เลือก **Music Bot Add-on** เพื่อเพิ่มจำนวน Music Bot (เพิ่ม maxMusicBots)

---

## 💻 การทำงานของระบบ

### 1. Database Schema

```prisma
model ServerDB {
  // ... fields อื่นๆ
  maxMusicBots Int @default(1) // จำนวน Music Bot สูงสุดที่ใช้ได้
}
```

### 2. ฟังก์ชันสำคัญ

#### `assignBotsToGuild()` - Assign Music Bot
```typescript
async assignBotsToGuild(guildId: string, botCount: number, userId?: string)
```
- ตรวจสอบว่าเซิร์ฟเวอร์ลงทะเบียนแล้ว
- **ตรวจสอบว่า botCount ไม่เกิน maxMusicBots**
- Assign bots ที่ต้องการ

#### `getMusicBotUsage()` - ดูการใช้งาน
```typescript
async getMusicBotUsage(guildId: string)
```
ส่งคืน:
```typescript
{
  current: 2,      // กำลังใช้งาน
  limit: 5,        // ขีดจำกัด
  available: 3,    // เหลือที่สามารถใช้ได้
  percentage: 40   // เปอร์เซ็นต์การใช้งาน
}
```

#### `canAddMusicBots()` - ตรวจสอบว่าเพิ่มได้หรือไม่
```typescript
async canAddMusicBots(guildId: string, additionalCount: number = 1): Promise<boolean>
```

#### `increaseMusicBotLimit()` - เพิ่มขีดจำกัด
```typescript
async increaseMusicBotLimit(guildId: string, additionalBots: number)
```
- ใช้เมื่อซื้อ Music Bot Add-on
- เพิ่ม maxMusicBots ในฐานข้อมูล

### 3. Payment Service Integration

เมื่อชำระเงินสำเร็จ:

```typescript
private async assignMusicBotsForPackage(
  guildId: string,
  packageType: string,
  packageId: string,
  userId?: string,
)
```

**สำหรับ Main Package:**
- ตั้งค่า `maxMusicBots` = จำนวนตาม package
- Assign Music Bots ตามจำนวน

**สำหรับ Music Add-on:**
- เพิ่ม `maxMusicBots` += จำนวนที่ซื้อ
- Assign Music Bots เพิ่มเติม

---

## 🎨 UI/UX Features

### Progress Bar
```
🟢 การใช้งาน: 2/5 ตัว (40%)
████░░░░░░
```

### Status Indicators
- 🟢 **เขียว** - ใช้งานน้อยกว่า 80%
- 🟡 **เหลือง** - ใช้งาน 80-99%
- 🔴 **แดง** - ใช้งานครบ 100%

### คำเตือน

**เมื่อเหลือ 1 ตัว:**
```
💡 คำแนะนำ
Music Bot ของคุณใกล้เต็มแล้ว! พิจารณาอัพเกรด package 
หรือซื้อ Music Bot Add-on เพิ่มเติม
```

**เมื่อเต็ม:**
```
⚠️ ขีดจำกัดเต็ม
คุณใช้งาน Music Bot ครบตามขีดจำกัดแล้ว
ใช้คำสั่ง /server-buy-package เพื่ออัพเกรด package 
หรือซื้อ Music Bot Add-on
```

---

## 🚨 Error Messages

### เมื่อพยายาม Assign เกินขีดจำกัด

```
❌ เซิร์ฟเวอร์นี้สามารถใช้ Music Bot ได้สูงสุด 3 ตัว (ตาม package ที่ซื้อ)
กรุณาอัพเกรด package หรือซื้อ Music Bot Add-on เพิ่มเติม
```

### เมื่อเซิร์ฟเวอร์ไม่ได้ลงทะเบียน

```
❌ เซิร์ฟเวอร์ไม่ได้ลงทะเบียนในระบบ
```

---

## 📊 ตัวอย่างการใช้งาน

### Scenario 1: เซิร์ฟเวอร์ใหม่
1. ซื้อ Package 2 (Pro) → maxMusicBots = 2
2. Assign Music Bot 2 ตัว → สำเร็จ ✅
3. พยายาม Assign ตัวที่ 3 → ❌ เกินขีดจำกัด

### Scenario 2: อัพเกรด Package
1. ใช้ Package 1 (Starter) → maxMusicBots = 1
2. Assign Music Bot 1 ตัว → สำเร็จ ✅
3. อัพเกรดเป็น Package 3 (Premium) → maxMusicBots = 3
4. Assign Music Bot เพิ่มอีก 2 ตัว → สำเร็จ ✅

### Scenario 3: ซื้อ Add-on
1. ใช้ Package 2 (Pro) → maxMusicBots = 2
2. Assign Music Bot 2 ตัว → สำเร็จ ✅
3. ซื้อ Music Bot Add-on (+3 ตัว) → maxMusicBots = 5
4. Assign Music Bot เพิ่มอีก 3 ตัว → สำเร็จ ✅

---

## 🔍 Logging

ระบบจะ log ข้อมูลสำคัญ:

```typescript
// เมื่อตรวจสอบขีดจำกัด
[assignBotsToGuild] Assigning 3 bots to guild 123456789
[assignBotsToGuild] Found 2 existing assignments (limit: 5)

// เมื่อตั้งค่าขีดจำกัดใหม่
[assignMusicBotsForPackage] Set maxMusicBots to 3 for guild 123456789

// เมื่อเพิ่มขีดจำกัด
[increaseMusicBotLimit] Increasing limit for guild 123456789 by 5
[increaseMusicBotLimit] New limit: 8

// เมื่อเกินขีดจำกัด
[assignBotsToGuild] Requested 10 bots but server limit is 5
```

---

## 🛠️ การ Maintenance

### อัพเดทจำนวน Music Bot ในแพ็คเกจ

แก้ไขใน `src/payment/payment.service.ts`:

```typescript
// Main packages
const botCounts: Record<string, number> = { 
  '1': 1,   // Package 1 = 1 bot
  '2': 2,   // Package 2 = 2 bots
  '3': 3,   // Package 3 = 3 bots
  '4': 5    // Package 4 = 5 bots
};

// Music add-ons
const botCounts: Record<string, number> = { 
  '1': 3,   // Add-on 1 = +3 bots
  '2': 5,   // Add-on 2 = +5 bots
  '3': 9,   // Add-on 3 = +9 bots
  '4': 15,  // Add-on 4 = +15 bots
  '5': 25   // Add-on 5 = +25 bots
};
```

### ตั้งค่าขีดจำกัด Default

แก้ไขใน `prisma/schema.prisma`:

```prisma
model ServerDB {
  maxMusicBots Int @default(1) // เปลี่ยนค่า default ได้ที่นี่
}
```

หลังจากแก้ไข run:
```bash
pnpm prisma db push
pnpm prisma generate
```

---

## 📝 Checklist สำหรับ Admin

เมื่อมีปัญหาเกี่ยวกับ Music Bot:

- [ ] ตรวจสอบว่าเซิร์ฟเวอร์ลงทะเบียนแล้ว
- [ ] ตรวจสอบ `maxMusicBots` ในฐานข้อมูล
- [ ] ตรวจสอบจำนวน Music Bot ที่ assign แล้ว
- [ ] ตรวจสอบว่า package ที่ซื้อถูกต้อง
- [ ] ตรวจสอบ log การชำระเงิน
- [ ] ตรวจสอบ log การ assign bots

---

## 🎯 ข้อดีของระบบ

✅ **ควบคุมการใช้งานได้ชัดเจน** - จำกัดตาม package ที่ซื้อ  
✅ **แจ้งเตือนล่วงหน้า** - เตือนเมื่อใกล้เต็ม  
✅ **UI ที่เข้าใจง่าย** - Progress bar และ indicator ชัดเจน  
✅ **ยืดหยุ่น** - สามารถซื้อ add-on เพิ่มได้  
✅ **Log ครบถ้วน** - สามารถตรวจสอบได้ทุกขั้นตอน  

---

## 📞 Support

หากมีปัญหาเกี่ยวกับระบบ Music Bot:

1. ตรวจสอบสถานะด้วย `/music-bot-status`
2. ตรวจสอบ package ที่ซื้อ
3. ติดต่อ Admin ผ่าน Discord
4. ตรวจสอบ log ในระบบ

---

**สร้างเมื่อ:** 21 พฤศจิกายน 2025  
**เวอร์ชัน:** 1.0.0  
**ผู้พัฒนา:** Warayut Taekrathok

